package sauna.pathFollowingAlgorithm;

public class PathFollowingAlgorithm {
    public static void main(String[] args) {

        LoadMatrix loadMatrix = new LoadMatrix();
        String[][] matrix = loadMatrix.loadMatrix(1);
    }
}
